func foo(arg x, arg y)
(
	int m;
	return (x + y);
)

func main(void)
(

	int x;
	int y;
	int z;
	x = 5;
	y = 1;

	if((x == 0) && (y==1))
	(
		int z;
		z = foo(x,y);
	)
	else(
		m = 3;
	)
	
	return 0;

)
